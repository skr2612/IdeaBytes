const titles={dashboard:"Executive Dashboard",request:"Create Service Request",marketplace:"Vendor Marketplace",tracking:"Work Order Tracking",payment:"Approval & Payment",vendor:"Vendor Portal",iot:"IoT Future Layer"};function show(id){document.querySelectorAll(".screen").forEach(s=>s.classList.remove("active"));document.querySelectorAll(".nav").forEach(n=>n.classList.remove("active"));document.getElementById(id).classList.add("active");let nav=document.querySelector(`[data-screen="${id}"]`);if(nav)nav.classList.add("active");document.getElementById("title").textContent=titles[id]||"Executive Dashboard"}document.querySelectorAll("[data-screen]").forEach(b=>b.onclick=()=>show(b.dataset.screen));document.querySelectorAll("[data-go]").forEach(b=>b.onclick=()=>show(b.dataset.go));